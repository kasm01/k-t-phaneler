import zipfile

zip_path = "/content/OKX-bot-main.zip"
extract_path = "/content/OKX-bot"

with zipfile.ZipFile(zip_path, 'r') as zip_ref:
    zip_ref.extractall(extract_path)

print("Dosyalar başarıyla çıkarıldı.")
